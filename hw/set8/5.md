5. Helmholtz coils. Two circular coils of radius R carry the same
current I such that the planes of the two coils are parallel and
separated by a distance R. Their currents have a sense such that
in between the coils along their shared axis, the magnetic field
for each are parallel. Find the magnetic field at this center point
in between the coils along their shared center axis.

use the result from the biot savart problem from 3)
except here im having the B point +i meaning that the current points towards us at the top and away at bottom 

B = ihat mu0 I a2 / (2r3)

B = B1 + B2 (POS)

in our case a = R and r = sqrt(R2 + (R/2)2) = R / 2 rt(5)

so B1 = ihat mu0 I R2 / (2 * (R / 2 rt(5))3)
simplify
B1 = ihat 4 mu0 I / (5 rt(5) R)
so B = 2 B1 
    = ihat 8 mu0 I / (5 rt(5) R)