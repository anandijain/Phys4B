5. A circular loop of radius a and mass m carries a current through it of
I. The loop is in a uniform magnetic field that is horizontal. The loop is a
rest on a horizontal surface such that its area is flat on that surface. Find
the value of the current in the loop such that is just starts to lift off the
table on one side.


# soln

Tau = I alpha (sys = ring)      | alpha zero by problem and no net external torque
Tau_normal + Tau_gravity + Tau_B = 0

The pivot point is equal to the point where the normal force is applied so rvec = 0 thus no torque

for gravity:
choose the pivot point to be the point that is raising up check me but i think i can just make it the right edge P = [a, 0, 0]
Then the force applied is -mg jhat so the torque is 

det [
    i           j           k
    -a          0           0
    0           0           -m g
]
= jhat a m g

for Tau_B: 

F_B = q(v cross B) 
since dq/dl = lambda and lambda * v = I (C/m * m / s)
dF_B = I dl cross B 

r(theta) = [a cos theta, a sin theta, 0]
dr/dtheta = [- a sin(theta), a cos theta, 0]
dr = dl 

evaluate first cross
det [
    i               j               k
    -a sin theta, a cos theta       0
    B               0               0
]
= khat * - I B a cos theta dtheta 

this is a tiny piece of force dF_B 

dTau_B = rvec cross dF_B 

rvec = r(theta) above

the cross product becomes 

det [
    i                   j                   k
    a cos theta         a sin theta         0
    0                   0                   I B a cos theta dtheta
]
= ihat * I B a2 sin theta cos theta dtheta 
+ -jhat * I B a2 cos2 theta dtheta

we integrate this from 0 to 2pi 
apparently the ihat integral goes to zero 

we get Tau_B = -jhat * a2 B I pi 

so 
Tau_g + Tau_B = 0 
a m g - a2 B I pi = 0

I = (m g) / (B a pi)

notice that two different pivot points for the torque were used (center for B and edge for g) why is this valid

it is valid to move the pivot point of a torque only if the net force on the system is zero. in the case of the magnetic force, there is no net force exhibited 