# http://nebula2.deanza.edu/~newton/4B/4BE2018.jpg

# prob
ring of current I2 center origin, radius a in xy plane 
so the l(theta) = [a cos theta, a sin theta, 0]

line of current I1 at x = -d 

find the force of current 1 (I1)  on 2 F(1->2)

# soln

first we use amperes law to find the force a distance away from the line of current

we get B = mu0 I / (2 pi r)

so we want to find a small force 

based on the two step right hand rule it seems that the B field in the xy plane due to I1 is into the page (-khat)

dF = Idl cross B 

so in this formula the force on the loop is from B, but the Idl is from the ring itself

dl/dtheta = [-a sin theta, a cos theta, 0]

so i found B(x) = -khat * mu0 I / (2 pi (d + a cos theta))

but i tried to evaluate the cross with a b sin alpha. 

I dtheta [-a sin theta, a cos theta, 0] cross -khat * mu0 I / (2 pi (d + a cos theta))

and its clear that they are always perpendiclar i think so then

||Idl|| = I a dtheta 
||B|| =  mu0 I / (2 pi (d + a cos theta))   | i believe this is right since its just in one direction so the norm is itself

so dF = I1 a dtheta mu0 I2 / (2 pi (d + a cos theta))
F = int dF
    = mu0 I2 a / 2pi int dtheta / (d + a cos theta)

we are allowed to leave integrals unevaled

# above is incorrect because the direction of the dF changes so we need to evaluate the determinant to actualy get the dFvec 

the det [
    i               j               k
    -a sin          a cos           0
    0               0               -1

]

int [-a cos, -a sin, 0] * I1 a dtheta mu0 I2 / (2 pi (d + a cos theta))
-int [a cos, a sin, 0] * I1 a dtheta mu0 I2 / (2 pi (d + a cos theta))

and you're supposed to see that the jhat component dies by symmetry 
and the result is 
