# http://nebula2.deanza.edu/~newton/4B/4BE2P2W17.jpg 

loop current I1, radius a
loop2 I2 radius b 

a < b 

both currents clockwise. 
find the magnitude of force F(I1 -> I2 )

# solution

first we need to find the magnitude of B due to I1 and distance r = b 

do biot savart and get 
P = [x, 0, 0] | later sub x -> b
B(p) = I1 mu0 khat / 4 pi int (0, 2pi) a2 - b a cos theta d theta / r3      |

r is just the norm of P - l(theta)
where l(theta) traces the circle: (a cos, a sin, 0)

r = sqrt((b - a cos)2 + a2 sin2)

now that B(p) found we need to do 

dF_B = I2 dl x B 

but see that B is pointing k hat (2 step rhr ) I actually found khat with det but whatever
and that dl is in the xy plane so the vectors are perpendicular thus we can use the polar form 

dF_B = I2 dl B sin90 
F_B = int I2 dl B       | but B has constant magnitude by symmetry, I2 is constant so becomes I2 B int dl = I2 B L = I2 B 2 pi b 
F_B =
    = mu0 I1 I2 b / 2 * B   | sub in B (the big nasty integral from 0, 2pi)
