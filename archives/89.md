# http://nebula2.deanza.edu/~newton/4B/4BE1P1Winter17.jpg
# lesson: 

be careful, in this problem the integral needed to be evaluated but i skipped it 

# problem 

dialectric sphere (rad = Ro, dens=rho (uniform) ) with hollow (rad =Ri)

q = point(charge=-q, mass=m, pos = Ro rhat)

The charge is released from rest. find the time it will take q to move from the start of the hollow to the other side

# soln

find E(r < R) with gauss law. then apply W = Delta U (-q Delta V = Delta U) so that

we can do the line integral (have to, its inside sphere so the E is not k q / r2 ) for Delta V and then find Vf when integrating from Ro to Ri. 
Then Vf*t = 2Ri
t = 2 Ri/Vf just need to find it 

there is a ton of algebra but here is the highlights:

E(r) = rho (r3 - Ri3)/(3r2) rhat 
    = rho / 3 eps (r - Ri3/r2) 

W = Delta U (sys = -q )
-(-q) delta V = 1/2 m vf2 (KEi = 0 by rest)

dleta V = -int(Ro, Ri) E dot dl | note dl = dr rhat and E is radial too, so parallel
    = -rho / 3[int(Ro, Ri) r dr - Ri3 int(Ro, Ri) dr/r2]


this gives something really nasty but then 

W = q Delta V (but it looks like delta V is negative but i suspect that the evaluated integral is also negative so it cancels but not sure: YES it does)

We expect that closer to the center of the sphere is higher potential, so delta V should be positive since we are moving in. 
higher - lower = positive

The displacement IS in the direction of the Force so I believe Work should be positive. 

then 1/2 m vf2 = q Delta V
vf = sqrt(2 q Delta V / m)

then for the time just do vf t = 2 Ri
t = 2 Ri/ vf

