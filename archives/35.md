# http://nebula2.deanza.edu/~newton/4B/4BE1P2Fall08.jpg

# prob 
We have two identical rods(charge=Q, len=L)
rod1 has one end on origin pointing +y 
rod2 is on x axis with left end at [D, 0]

find vertical force of 2 perp rods on eachother
F(rod1->rod2)

# soln
do a int dE, throw away the dEx 
so we can do 

here dq = Q/L dy 
r = sqrt(x2 + y2)
sin(theta) = y/r 
dEy = k dq / r2 sin theta jhat   | minus sign? 
Ey(x) = int(0, L) k lam dy / r2 sin theta jhat

once we find dEy(x)

we can integrate dq/dx = Q/L= lam 
int(0, L) E dq 
    = int(D, D+L) E Q / L dx 

getting tired and lazy
 
Fy(rod1->rod2)= k Q2 / L2 int(D, D+L) (1/x - 1/(sqrt(x2 + L2)))dx

nasty integral, can skip