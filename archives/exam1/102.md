# http://nebula2.deanza.edu/~newton/4B/4BE1031.jpg

# i need to redo the torque oscillation problem 

# problem 

We are given two charges
q1 = charge(q, [0, d/2, 0])
q2 = charge(q, [0, -d/2, 0])

and then a small bead(mass=m, charge = -q, [x,0])

given F = k Delta(x) gives a period of T = 2pi sqrt(m/k)    | makes sense when m is bigger the period is bigger (longer)

